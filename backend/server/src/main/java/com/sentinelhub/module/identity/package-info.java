/**
 * Business module: identity
 * <p>
 * Internal business logic — called by api.admin / api.app / api.agent layers.
 * Not exposed as independent microservice.
 */
package com.sentinelhub.module.identity;
